import data from "../../data/noda.json";

export function getTimeZoneNames(){
    let id = []
    let ali = []
    let aligp = []

    data.zones.forEach((d)=>{
        const a = d.aliases.map((i)=> i)
    })
}